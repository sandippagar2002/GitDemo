pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                echo 'Hello world!' 
                echo "Current Build Number ${currentBuild.number}"
                echo 'Current Build Number ${currentBuild.number}'
                echo "My Name is ${myName}"
            }
        }
    }
}
